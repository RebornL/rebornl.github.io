# 长城基金面试

笔试：排序算法和数据结构，计算机网络，数据库，算法，数学

面试：三个面试旁边有个HR在听，每个面试官轮流问，对着你简历，进行提问



个人性格上的缺点：可能不善言辞，特别是跟不熟悉的人，说话会变得紧张。

个人技术上的缺点：没有经历过真正高并发，高压的场景，所以觉得自己的技术深度可能还不够。


### 关于数据库
一道数据库的题目：详细可以看[这篇文章](<https://blog.csdn.net/waiwai021/article/details/50171669>)

**表中有A B C三列,用SQL语句实现：当A列大于B列时选择A列否则选择B列，当B列大于C列时选择B列否则选择C列。**

这道题主要用到`case`函数

```sql
select case where a>b then a
			where b>c then b
		else c
from table;
```





另外一个数据库的知识点记录：

关于分组的使用，GROUP BY把具有相同的数据值的行放到一起，HAVING对分组进行过滤，WHERE则是对行进行过滤

```sql
SELECT col, COUNT(*) AS num
FROM mytable
WHERE col > 2
GROUP BY col
HAVING num >= 2;
```

COUNT(\*) 是统计相同分组中的个数，GROUP BY一般用在WHERE自语句

### 子网地址，广播地址和网段的个数

- 子网地址：IP地址与子网掩码进行按位与运算得到子网地址，显示为这样203.123.1.128/26
- 广播地址：子网掩码取反后与子网地址进行或运算就可以得到广播地址，上面这个子网地址的广播地址就是203.123.1.191
- IP地址分配：除去子网和广播地址
(详细可以查看，我整理的知识总结关于网络部分)
































