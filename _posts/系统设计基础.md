# 系统设计基础

- 性能指标
  - 响应时间
  - 吞吐量
  - 并发用户数
- 性能优化
  - 集群方式
  - 缓存
  - 异步
- 伸缩性和性能区别
  1. 存在性能问题，那单个用户请求总是很慢。
  2. 存在伸缩性问题，那单个用户请求在并发情况下会很慢。
- 扩展性：添加新功能时，对现有系统的其他应用无影响，这要求不同应用具有低耦合的特性
  - 消息队列
  - 分布式服务将业务和可复用服务分离开来
- 可用性
  - 冗余
  - 监控
  - 服务降级
- 安全性



# 消息队列

- 消息模型

  - 点对点：消息生产者向消息队列中发送一个消息，该消息只能被消费一次

  - 发布和订阅：消费生产者向频道发送一个消息之后，多个消费者可以向频道订阅该消息

    > 与**观察者模式**的区别：观察者和主题都知道对方的存在；而在发布订阅模式中，都不知道对方存在，只通过频道进行通信；观察者模式是同步的，事件触发后会等待方法的返回，而订阅和发布模式则是异步的，消息的发布和被谁订阅都不关心。

  - 使用场景：

    - 异步处理
    - 流量削峰
    - 应用解耦

  - 如何保证可靠性

    **发送的可靠性**

    > 在本地数据库建一张消息表，将消息数据与业务数据保存在同一数据库实例里，这样就可以**利用本地数据库的事务机制**。事务提交成功后，将消息表中的消息转移到消息队列中，若转移消息成功则删除消息表中的数据，否则继续重传。

    **接收的可靠性**